# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# local database — the operator's live data lives here, never commit it
/data/*.db
/data/*.db-shm
/data/*.db-wal

# backups — timestamped local backups, never commit
/backups/*.db

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
.env

# typescript
next-env.d.ts
*.tsbuildinfo
